---
mnema:
  id: 019f08e0-7418-754d-89e9-d246197fb197
  kind: observation
  content: >-
    NOTA-25 (Sprint 6): página de Grafo /grafo com @xyflow/react (React Flow 12)
    + @dagrejs/dagre + html-to-image. grafo/layout.ts: aplicarLayout (dagre LR)
    + mesclarGrafo que converte a resposta de /empresa/:cnpj/grafo em
    nós/arestas React Flow mesclando SEM duplicar (Map por id). NoCustom.tsx: nó
    circular colorido por tipo (empresa azul/nf verde/produto laranja).
    GraphPanel.tsx: painel universal de detalhes (por ora foca Empresa;
    NF/Produto quando expansão trouxer esses tipos). Grafo.tsx: estado
    consolidado {nodes,edges} num único useState (evita loop de deps no carregar
    — callback estável com deps []); toolbar busca/slider
    depth(useGraphStore)/direção/reset/exportar PNG; onNodeClick expande depth=1
    mesclando + abre painel; ?cnpj= via validateSearch na rota é a fonte de
    verdade (useEffect reage a search.cnpj). ACHADO de lint: o projeto NÃO tem o
    plugin eslint react-hooks, então um comentário eslint-disable-next-line
    react-hooks/exhaustive-deps vira ERRO (regra inexistente) — removido. Rota
    /grafo registrada (substituiu placeholder). vite build OK, app HTTP 200 em
    /grafo, test:unit 36/36, typecheck/lint/build raiz verdes. Validação é
    estrutural (build+app sobe); interação visual real virá nos e2e Playwright
    (NOTA-27).
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-27T11:39:15.352Z'
  archived_at: null
---
NOTA-25 (Sprint 6): página de Grafo /grafo com @xyflow/react (React Flow 12) + @dagrejs/dagre + html-to-image. grafo/layout.ts: aplicarLayout (dagre LR) + mesclarGrafo que converte a resposta de /empresa/:cnpj/grafo em nós/arestas React Flow mesclando SEM duplicar (Map por id). NoCustom.tsx: nó circular colorido por tipo (empresa azul/nf verde/produto laranja). GraphPanel.tsx: painel universal de detalhes (por ora foca Empresa; NF/Produto quando expansão trouxer esses tipos). Grafo.tsx: estado consolidado {nodes,edges} num único useState (evita loop de deps no carregar — callback estável com deps []); toolbar busca/slider depth(useGraphStore)/direção/reset/exportar PNG; onNodeClick expande depth=1 mesclando + abre painel; ?cnpj= via validateSearch na rota é a fonte de verdade (useEffect reage a search.cnpj). ACHADO de lint: o projeto NÃO tem o plugin eslint react-hooks, então um comentário eslint-disable-next-line react-hooks/exhaustive-deps vira ERRO (regra inexistente) — removido. Rota /grafo registrada (substituiu placeholder). vite build OK, app HTTP 200 em /grafo, test:unit 36/36, typecheck/lint/build raiz verdes. Validação é estrutural (build+app sobe); interação visual real virá nos e2e Playwright (NOTA-27).
