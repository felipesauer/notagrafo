---
mnema:
  id: 019f112c-05fe-742e-927f-dcc544df8e85
  kind: observation
  content: >-
    Ambos os bugs e2e da auditoria 2 corrigidos na branch feat/sprint-9-coverage
    (PR #10). NOTA-48 (grafo): o default do TanStack Router serializa search com
    JSON (gera ?cnpj=%22...%22) E o parseSearchWith COAGE números — o CNPJ
    virava number e o validateSearch (typeof==='string') o descartava, então o
    grafo não carregava. Solução: serializadores custom em router.tsx
    (stringifySearch/parseSearch exportados) baseados em URLSearchParams —
    escalares viram string crua (sem aspas, sem coerção numérica),
    objetos/arrays usam JSON. Cuidado: parseSearchWith do TanStack tem fast-path
    que coage número ANTES do parser custom, por isso NÃO dá para usar
    parseSearchWith — tem que ser parser próprio. NOTA-49 (login): apiFetch
    tratava TODO 401 como sessão expirada
    (refresh→clearToken→onUnauthorized→redirect /login), atropelando o alert e
    trocando INVALID_CREDENTIALS por 'Sessão expirada'. Solução: opção
    skipAuthRefresh em RequestOptions, ativada no POST /auth/login. Resultado:
    suite e2e completa 12/12 contra docker; unit subiu para 159
    (router.search.test + api.client.test), cobertura 93,89% lines. Reprodução
    e2e exige .env (DEMO=true, REDIS_PORT=6390 p/ evitar conflito com redis do
    host) + docker compose --profile app --profile demo + seed (cria usuário
    demo).
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-29T02:18:45.630Z'
  archived_at: null
---
Ambos os bugs e2e da auditoria 2 corrigidos na branch feat/sprint-9-coverage (PR #10). NOTA-48 (grafo): o default do TanStack Router serializa search com JSON (gera ?cnpj=%22...%22) E o parseSearchWith COAGE números — o CNPJ virava number e o validateSearch (typeof==='string') o descartava, então o grafo não carregava. Solução: serializadores custom em router.tsx (stringifySearch/parseSearch exportados) baseados em URLSearchParams — escalares viram string crua (sem aspas, sem coerção numérica), objetos/arrays usam JSON. Cuidado: parseSearchWith do TanStack tem fast-path que coage número ANTES do parser custom, por isso NÃO dá para usar parseSearchWith — tem que ser parser próprio. NOTA-49 (login): apiFetch tratava TODO 401 como sessão expirada (refresh→clearToken→onUnauthorized→redirect /login), atropelando o alert e trocando INVALID_CREDENTIALS por 'Sessão expirada'. Solução: opção skipAuthRefresh em RequestOptions, ativada no POST /auth/login. Resultado: suite e2e completa 12/12 contra docker; unit subiu para 159 (router.search.test + api.client.test), cobertura 93,89% lines. Reprodução e2e exige .env (DEMO=true, REDIS_PORT=6390 p/ evitar conflito com redis do host) + docker compose --profile app --profile demo + seed (cria usuário demo).
