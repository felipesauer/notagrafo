---
mnema:
  id: 019f13d3-3ce2-770f-9b35-33e429b5cba0
  kind: observation
  content: >-
    Executado NOTA-EPIC-10 (padronização PT→EN de identificadores de código) na
    branch feat/code-i18n-en, 5 commits (1 por pacote, na ordem das dependências
    core→graph→{api,worker}→dashboard): a36e81e core, a72bcb6 graph, 95ff3b7
    api, 7f2b3c3 worker, 7fadb8b dashboard. Estratégia: branch única com commit
    por pacote, refs cruzadas atualizadas a cada commit para manter a branch
    sempre compilável. Baseline final: build dos 5 pacotes OK, lint global
    limpo, 159/159 unit verdes. Integração (66) e e2e exigem
    Testcontainers/docker e NÃO foram rodados nesta sessão — recomenda-se rodar
    antes do merge. PR ainda não aberto.


    Descobertas/decisões de escopo durante a execução: (1) os tipos de RESPOSTA
    do graph (EmpresaGrafo/EmpresaStats/ProdutoRanking) são retornados direto
    pela api ao dashboard (empresa.routes.ts faz `return getCompanyGraph(...)`),
    então seus campos
    (nos/arestas/relacao/grau/porUf/ranking/meta.filtrosAtivos) são CONTRATO e
    foram mantidos — só os nomes de FUNÇÃO mudaram. (2) ExportJob é serializado
    no Redis (sobrevive a restart) e seus campos vão pro JSON de resposta —
    campos mantidos, só métodos do ExportService renomeados (create/get/read).
    (3) Label Neo4j `:Usuario` mantido (tipo TS Usuario→User). (4) Paths de URL
    do dashboard (/empresas,/produtos,/grafo,/exportacoes,/configuracoes) e
    search param cnpj mantidos em PT (contrato visível). (5) Chave i18n
    grafo.exportarPng→exportPng alinhada nos 2 locales+JSX (valores de UI
    intactos). Não cobertos por baixo valor: props de shared.tsx
    (valor/label/linhas), nomes de var de rota no router, comentários PT. Bugs
    de typecheck PRÉ-EXISTENTES em test files de integração (api counts.*
    possibly undefined, worker tuplas vazias) confirmados na base via git stash
    — NÃO corrigidos (fora de escopo). O working tree da branch trouxe mudanças
    pré-existentes não relacionadas (docker-compose, package.json, server.ts,
    worker.ts regex entrypoint, api.client) que foram deliberadamente EXCLUÍDAS
    dos commits.
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-29T14:40:38.626Z'
  archived_at: null
---
Executado NOTA-EPIC-10 (padronização PT→EN de identificadores de código) na branch feat/code-i18n-en, 5 commits (1 por pacote, na ordem das dependências core→graph→{api,worker}→dashboard): a36e81e core, a72bcb6 graph, 95ff3b7 api, 7f2b3c3 worker, 7fadb8b dashboard. Estratégia: branch única com commit por pacote, refs cruzadas atualizadas a cada commit para manter a branch sempre compilável. Baseline final: build dos 5 pacotes OK, lint global limpo, 159/159 unit verdes. Integração (66) e e2e exigem Testcontainers/docker e NÃO foram rodados nesta sessão — recomenda-se rodar antes do merge. PR ainda não aberto.

Descobertas/decisões de escopo durante a execução: (1) os tipos de RESPOSTA do graph (EmpresaGrafo/EmpresaStats/ProdutoRanking) são retornados direto pela api ao dashboard (empresa.routes.ts faz `return getCompanyGraph(...)`), então seus campos (nos/arestas/relacao/grau/porUf/ranking/meta.filtrosAtivos) são CONTRATO e foram mantidos — só os nomes de FUNÇÃO mudaram. (2) ExportJob é serializado no Redis (sobrevive a restart) e seus campos vão pro JSON de resposta — campos mantidos, só métodos do ExportService renomeados (create/get/read). (3) Label Neo4j `:Usuario` mantido (tipo TS Usuario→User). (4) Paths de URL do dashboard (/empresas,/produtos,/grafo,/exportacoes,/configuracoes) e search param cnpj mantidos em PT (contrato visível). (5) Chave i18n grafo.exportarPng→exportPng alinhada nos 2 locales+JSX (valores de UI intactos). Não cobertos por baixo valor: props de shared.tsx (valor/label/linhas), nomes de var de rota no router, comentários PT. Bugs de typecheck PRÉ-EXISTENTES em test files de integração (api counts.* possibly undefined, worker tuplas vazias) confirmados na base via git stash — NÃO corrigidos (fora de escopo). O working tree da branch trouxe mudanças pré-existentes não relacionadas (docker-compose, package.json, server.ts, worker.ts regex entrypoint, api.client) que foram deliberadamente EXCLUÍDAS dos commits.
