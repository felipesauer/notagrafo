---
mnema:
  id: 019f075d-8c5f-750b-b58f-e6eacef808ae
  kind: observation
  content: >-
    NOTA-21 (fecha Sprint 5 API): criado
    packages/api/src/__tests__/setup.integration.ts — setupApiIntegration()
    reutilizável que sobe Neo4j+Redis (Testcontainers), injeta
    NEO4J_*/REDIS_URL/AUTH_SECRET no env (como o setup do 04 infra-testes.md),
    roda migrations, cria usuário de teste, monta a API completa e faz login;
    retorna ApiTestContext
    {app,driver,redis,queue,storage,token,bearer(),clearDatabase(),teardown()}.
    api.integration.test.ts usa esse setup com afterEach=clearDatabase (MATCH
    (n) WHERE NOT n:Usuario DETACH DELETE n + queue obliterate) cobrindo upload
    XML/ZIP/inválido/duplicata, lista+filtros, detalhe, grafo, export e2e. Os
    testes de integração anteriores (auth/nf/routes/rate-limit) continuam com
    seu próprio boot — não refatorei para o setup compartilhado para não
    arriscar regressão; o setup é o helper canônico para novos testes. test:unit
    36/36, test:integration 53/53 (11 arquivos). Sprint 5 (API) 100%
    implementada.
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-27T04:36:39.135Z'
  archived_at: null
---
NOTA-21 (fecha Sprint 5 API): criado packages/api/src/__tests__/setup.integration.ts — setupApiIntegration() reutilizável que sobe Neo4j+Redis (Testcontainers), injeta NEO4J_*/REDIS_URL/AUTH_SECRET no env (como o setup do 04 infra-testes.md), roda migrations, cria usuário de teste, monta a API completa e faz login; retorna ApiTestContext {app,driver,redis,queue,storage,token,bearer(),clearDatabase(),teardown()}. api.integration.test.ts usa esse setup com afterEach=clearDatabase (MATCH (n) WHERE NOT n:Usuario DETACH DELETE n + queue obliterate) cobrindo upload XML/ZIP/inválido/duplicata, lista+filtros, detalhe, grafo, export e2e. Os testes de integração anteriores (auth/nf/routes/rate-limit) continuam com seu próprio boot — não refatorei para o setup compartilhado para não arriscar regressão; o setup é o helper canônico para novos testes. test:unit 36/36, test:integration 53/53 (11 arquivos). Sprint 5 (API) 100% implementada.
