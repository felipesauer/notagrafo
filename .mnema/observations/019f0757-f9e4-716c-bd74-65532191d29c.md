---
mnema:
  id: 019f0757-f9e4-716c-bd74-65532191d29c
  kind: observation
  content: >-
    NOTA-20 (Sprint 5): rotas empresa/stats/export/health no @notagrafo/api.
    health.routes.ts: GET /health SEM auth e FORA do /api/v1 (Docker), checa
    neo4j/redis/storage em paralelo, 200/503, xsdVersions=versoesSuportadas().
    empresa.routes.ts: /empresa/:cnpj (props do nó + getEmpresaStats), /grafo
    valida depth 1..4 ANTES da query (400). stats.routes.ts:
    overview/volume(substring de dataEmissao por
    granularidade)/top-empresas/top-produtos(reusa topProdutos). export:
    ExportService (export/export.service.ts) gera arquivo em disco temp a partir
    de listNotasFiscais paginando até o fim; csv/json (xlsx servido como CSV
    tabular no MVP); jobs em memória por instância com
    expiresAt=now+EXPORT_TTL_HOURS; obter() devolve 'expired' e remove arquivo
    após TTL → 410. buildApp ganhou opção redis (p/ health). 9 testes integração
    + 2 unit (TTL/410). test:unit 36/36, test:integration 46/46. Decisão:
    geração de export é síncrona-em-background dentro da API (Map em memória) em
    vez de fila BullMQ dedicada — o plano não detalha worker de export e isso é
    suficiente p/ MVP single-node; se escalar, migrar para job BullMQ.
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-27T04:30:33.956Z'
  archived_at: null
---
NOTA-20 (Sprint 5): rotas empresa/stats/export/health no @notagrafo/api. health.routes.ts: GET /health SEM auth e FORA do /api/v1 (Docker), checa neo4j/redis/storage em paralelo, 200/503, xsdVersions=versoesSuportadas(). empresa.routes.ts: /empresa/:cnpj (props do nó + getEmpresaStats), /grafo valida depth 1..4 ANTES da query (400). stats.routes.ts: overview/volume(substring de dataEmissao por granularidade)/top-empresas/top-produtos(reusa topProdutos). export: ExportService (export/export.service.ts) gera arquivo em disco temp a partir de listNotasFiscais paginando até o fim; csv/json (xlsx servido como CSV tabular no MVP); jobs em memória por instância com expiresAt=now+EXPORT_TTL_HOURS; obter() devolve 'expired' e remove arquivo após TTL → 410. buildApp ganhou opção redis (p/ health). 9 testes integração + 2 unit (TTL/410). test:unit 36/36, test:integration 46/46. Decisão: geração de export é síncrona-em-background dentro da API (Map em memória) em vez de fila BullMQ dedicada — o plano não detalha worker de export e isso é suficiente p/ MVP single-node; se escalar, migrar para job BullMQ.
