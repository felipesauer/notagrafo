---
mnema:
  id: 019f0645-93f0-712a-89fa-4dd67c1ece23
  kind: observation
  content: >-
    NOTA-17 (Sprint 5): auth JWT manual no @notagrafo/api (ADR-1).
    auth.plugin.ts registra @fastify/jwt (AUTH_SECRET,
    expiresIn=AUTH_JWT_EXPIRES_IN) e decora app.authenticate
    (jwtVerify→ApiError.unauthorized). Augmentations: FastifyJWT.payload/user e
    FastifyInstance.authenticate. auth.routes.ts: login (bcrypt.compare; 401
    INVALID_CREDENTIALS), logout 204 stateless, refresh (verify+sign novo), me
    (preHandler authenticate). user.repository.ts: usuários como nós :Usuario no
    Neo4j, senha bcrypt.hash salt 10. buildApp ganhou opção driver — quando
    presente registra authPlugin (raiz, decorator herda nos filhos) e authRoutes
    sob /api/v1; sem driver, app sobe sem auth (testes base). server.ts roda
    migrations + passa driver. expiresAt extraído do exp do token via
    app.jwt.decode. 7 testes integração Neo4j real (28 integration total).
    @types/bcryptjs é deprecated (bcryptjs já tem tipos) mas inofensivo.
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-26T23:30:50.992Z'
  archived_at: null
---
NOTA-17 (Sprint 5): auth JWT manual no @notagrafo/api (ADR-1). auth.plugin.ts registra @fastify/jwt (AUTH_SECRET, expiresIn=AUTH_JWT_EXPIRES_IN) e decora app.authenticate (jwtVerify→ApiError.unauthorized). Augmentations: FastifyJWT.payload/user e FastifyInstance.authenticate. auth.routes.ts: login (bcrypt.compare; 401 INVALID_CREDENTIALS), logout 204 stateless, refresh (verify+sign novo), me (preHandler authenticate). user.repository.ts: usuários como nós :Usuario no Neo4j, senha bcrypt.hash salt 10. buildApp ganhou opção driver — quando presente registra authPlugin (raiz, decorator herda nos filhos) e authRoutes sob /api/v1; sem driver, app sobe sem auth (testes base). server.ts roda migrations + passa driver. expiresAt extraído do exp do token via app.jwt.decode. 7 testes integração Neo4j real (28 integration total). @types/bcryptjs é deprecated (bcryptjs já tem tipos) mas inofensivo.
