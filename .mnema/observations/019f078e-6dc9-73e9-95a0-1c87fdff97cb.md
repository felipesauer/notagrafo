---
mnema:
  id: 019f078e-6dc9-73e9-95a0-1c87fdff97cb
  kind: observation
  content: >-
    NOTA-24 (maior task, estimate 13): 5 páginas de dados do dashboard.
    shared.tsx (NFStatusBadge, CNPJText, CurrencyValue, DateDisplay, EmptyState,
    LoadingSkeleton, InlineError, ErrorBoundary). hooks.ts (TanStack Query:
    useOverview/useVolume/useTopEmpresas/useNFList/useNFDetail/useEmpresa/useTopProdutos
    com helper qs). Overview: 4 KPIs + Recharts ComposedChart(volume) +
    BarChart(top fornecedores) + tabela. NFList: filtros (q/status) + paginação
    cursor (pilha de cursores client-side) + UploadModal (FormData multipart
    direto no fetch, não pelo apiFetch, por ser binário). NFDetail: 2 colunas,
    mini-grafo é link p/ /grafo?cnpj (grafo real é NOTA-25). Empresas/Produtos:
    tabela + card inline expansível (linha clicável). Achados TS: cast
    Record→Produto[] precisa as unknown as; Link to com path dinâmico
    (/nf/$chave) e search precisaram as string/as never por causa do router
    code-based sem plugin de tipos. Recharts instalado. Páginas registradas no
    router (5 reais + grafo/export/config ainda Placeholder). vite build OK, app
    HTTP 200, test:unit 36/36, typecheck/lint/build raiz verdes. Não há testes
    automatizados de UI ainda — validação visual real virá nos e2e Playwright
    (NOTA-27).
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-27T05:30:02.569Z'
  archived_at: null
---
NOTA-24 (maior task, estimate 13): 5 páginas de dados do dashboard. shared.tsx (NFStatusBadge, CNPJText, CurrencyValue, DateDisplay, EmptyState, LoadingSkeleton, InlineError, ErrorBoundary). hooks.ts (TanStack Query: useOverview/useVolume/useTopEmpresas/useNFList/useNFDetail/useEmpresa/useTopProdutos com helper qs). Overview: 4 KPIs + Recharts ComposedChart(volume) + BarChart(top fornecedores) + tabela. NFList: filtros (q/status) + paginação cursor (pilha de cursores client-side) + UploadModal (FormData multipart direto no fetch, não pelo apiFetch, por ser binário). NFDetail: 2 colunas, mini-grafo é link p/ /grafo?cnpj (grafo real é NOTA-25). Empresas/Produtos: tabela + card inline expansível (linha clicável). Achados TS: cast Record→Produto[] precisa as unknown as; Link to com path dinâmico (/nf/$chave) e search precisaram as string/as never por causa do router code-based sem plugin de tipos. Recharts instalado. Páginas registradas no router (5 reais + grafo/export/config ainda Placeholder). vite build OK, app HTTP 200, test:unit 36/36, typecheck/lint/build raiz verdes. Não há testes automatizados de UI ainda — validação visual real virá nos e2e Playwright (NOTA-27).
