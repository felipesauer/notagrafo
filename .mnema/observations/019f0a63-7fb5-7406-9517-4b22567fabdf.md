---
mnema:
  id: 019f0a63-7fb5-7406-9517-4b22567fabdf
  kind: observation
  content: >-
    AUDITORIA notagrafo — BUGS/GAPS REAIS (validados código vs .plan).
    DIVERGÊNCIAS DE CONTRATO API (média-alta): (1) GET /nf/jobs/:jobId retorna
    só {jobId,status,progresso,erro?} — falta
    total/iniciadoEm/concluidoEm/resultado{processadas,duplicatas,erros}
    (contrato seção 3). (2) GET /nf não retorna meta:{total,filtrosAtivos}
    (seção 4). (3) GET /nf/:chave/eventos sem chaveAcesso no topo, expõe
    'detalhes' não-contratado, timestamp via toString() pode não ser ISO8601
    puro. (4) GET /export/:id não retorna progresso/total quando processing.
    GAPS UI (dashboard vs .plan/03): (5) Overview sem Treemap por UF. (6)
    FilterSidebar inexistente — NFList tem 2 filtros (status+q) dos ~13 do
    contrato. (7) UploadModal sem drag-and-drop e sem polling do job (3s) com
    resumo. ROBUSTEZ: POST /nf/upload de ZIP não é atômico (enfileira alguns
    antes de detectar duplicata no meio); seed engole erros. COBERTURA:
    test:unit ~38% linhas — sem teste unit/integração: getNotaFiscal,
    historicoPrecoProduto, GET /nf/jobs/:jobId, upload ZIP múltiplo. PRIORIDADE:
    contrato API (1-4) maior impacto p/ cliente externo; UI (5-7) features do
    plano não entregues. Nenhum bug crítico no fluxo principal (pipeline e2e e
    quickstart 15/15 OK).
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-27T18:42:00.757Z'
  archived_at: null
---
AUDITORIA notagrafo — BUGS/GAPS REAIS (validados código vs .plan). DIVERGÊNCIAS DE CONTRATO API (média-alta): (1) GET /nf/jobs/:jobId retorna só {jobId,status,progresso,erro?} — falta total/iniciadoEm/concluidoEm/resultado{processadas,duplicatas,erros} (contrato seção 3). (2) GET /nf não retorna meta:{total,filtrosAtivos} (seção 4). (3) GET /nf/:chave/eventos sem chaveAcesso no topo, expõe 'detalhes' não-contratado, timestamp via toString() pode não ser ISO8601 puro. (4) GET /export/:id não retorna progresso/total quando processing. GAPS UI (dashboard vs .plan/03): (5) Overview sem Treemap por UF. (6) FilterSidebar inexistente — NFList tem 2 filtros (status+q) dos ~13 do contrato. (7) UploadModal sem drag-and-drop e sem polling do job (3s) com resumo. ROBUSTEZ: POST /nf/upload de ZIP não é atômico (enfileira alguns antes de detectar duplicata no meio); seed engole erros. COBERTURA: test:unit ~38% linhas — sem teste unit/integração: getNotaFiscal, historicoPrecoProduto, GET /nf/jobs/:jobId, upload ZIP múltiplo. PRIORIDADE: contrato API (1-4) maior impacto p/ cliente externo; UI (5-7) features do plano não entregues. Nenhum bug crítico no fluxo principal (pipeline e2e e quickstart 15/15 OK).
