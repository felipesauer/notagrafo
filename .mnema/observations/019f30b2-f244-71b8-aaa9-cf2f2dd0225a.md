---
mnema:
  id: 019f30b2-f244-71b8-aaa9-cf2f2dd0225a
  kind: observation
  content: >-
    Revisão FINAL da Fase 7 (commit a0b1954, PR #53): (A) varredura e2e MANUAL
    de 19 fluxos dirigindo o app na 8080 (home/explorar todas
    entidades/grafo/nfdetail/exports/config/tema escuro/mobile+sheet/Cmd+K) —
    ZERO erros de console/pageerror/HTTP≥400 em todas as telas e temas. (B)
    code-review do diff da Fase 7 (~730 linhas, backend+grafo+frontend) com 2
    finders. Achado 1 (BUG REAL confirmado empiricamente): 'Limpar tudo' dos
    NFFilters só limpava 1 filtro — campos.forEach(setFiltro('')) faz N navigate
    do MESMO snapshot de search → sobrescrevem, vence a última (ncm). Fix: um
    único navigate removendo todos os campos (função limparFiltros no Explorer).
    LIÇÃO reutilizável: nunca chamar navigate/setSearch em loop com spread do
    mesmo search — batch num navigate só. Achado 2 (a11y): ProdutoPeek sem
    SheetTitle (warning Radix) + X duplicado (SheetContent já traz close default
    via showCloseButton=true) → usar SheetHeader/SheetTitle, não custom X.
    Achado 3 (consistência): query de notas do grafo não excluía devoluções →
    add coalesce(finalidade,'')<>'devolucao'. Verificado que nomes de filtro
    front↔API batem 100% (nenhum filtro silenciosamente ignorado, que era o
    risco nº1). Redesign+auditoria+polish+revisão = COMPLETO. PR #53, 14
    commits, 13/13 e2e, aguardando merge humano.
  topics:
    - code-review
    - e2e
    - revisao
    - fase7
    - bugfix
  related_task_key: NOTA-125
  created_by: 019f0164-3101-76bc-af75-94e9b1380134
  at: '2026-07-05T05:14:21.636Z'
  archived_at: null
---
Revisão FINAL da Fase 7 (commit a0b1954, PR #53): (A) varredura e2e MANUAL de 19 fluxos dirigindo o app na 8080 (home/explorar todas entidades/grafo/nfdetail/exports/config/tema escuro/mobile+sheet/Cmd+K) — ZERO erros de console/pageerror/HTTP≥400 em todas as telas e temas. (B) code-review do diff da Fase 7 (~730 linhas, backend+grafo+frontend) com 2 finders. Achado 1 (BUG REAL confirmado empiricamente): 'Limpar tudo' dos NFFilters só limpava 1 filtro — campos.forEach(setFiltro('')) faz N navigate do MESMO snapshot de search → sobrescrevem, vence a última (ncm). Fix: um único navigate removendo todos os campos (função limparFiltros no Explorer). LIÇÃO reutilizável: nunca chamar navigate/setSearch em loop com spread do mesmo search — batch num navigate só. Achado 2 (a11y): ProdutoPeek sem SheetTitle (warning Radix) + X duplicado (SheetContent já traz close default via showCloseButton=true) → usar SheetHeader/SheetTitle, não custom X. Achado 3 (consistência): query de notas do grafo não excluía devoluções → add coalesce(finalidade,'')<>'devolucao'. Verificado que nomes de filtro front↔API batem 100% (nenhum filtro silenciosamente ignorado, que era o risco nº1). Redesign+auditoria+polish+revisão = COMPLETO. PR #53, 14 commits, 13/13 e2e, aguardando merge humano.
