---
mnema:
  id: 019f050e-6395-723c-9826-20185bed769a
  kind: observation
  content: >-
    NOTA-13 (início Sprint 4): módulo de storage de XML em @notagrafo/worker.
    Interface XmlStorage {save,get,exists} com factory createXmlStorage(env) por
    XML_STORAGE_DRIVER (padrão minio). Decisão: s3 e minio compartilham UMA
    implementação (S3XmlStorage com @aws-sdk/client-s3) — MinIO é S3-compatível,
    só muda endpoint + forcePathStyle:true. Local usa fs/promises. Testado:
    local via unit (fs temp), minio via integration com @testcontainers/minio
    (precisa criar o bucket com CreateBucketCommand antes de usar).
    @testcontainers/minio expõe getConnectionUrl()/getUsername()/getPassword().
    worker ganhou dep @aws-sdk/client-s3.
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-26T17:50:56.917Z'
  archived_at: null
---
NOTA-13 (início Sprint 4): módulo de storage de XML em @notagrafo/worker. Interface XmlStorage {save,get,exists} com factory createXmlStorage(env) por XML_STORAGE_DRIVER (padrão minio). Decisão: s3 e minio compartilham UMA implementação (S3XmlStorage com @aws-sdk/client-s3) — MinIO é S3-compatível, só muda endpoint + forcePathStyle:true. Local usa fs/promises. Testado: local via unit (fs temp), minio via integration com @testcontainers/minio (precisa criar o bucket com CreateBucketCommand antes de usar). @testcontainers/minio expõe getConnectionUrl()/getUsername()/getPassword(). worker ganhou dep @aws-sdk/client-s3.
