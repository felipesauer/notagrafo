---
mnema:
  id: 019f074e-4fe7-776f-908f-bfbfe49b272b
  kind: observation
  content: >-
    NOTA-19 (Sprint 5, maior task): rotas de NF no @notagrafo/api. nf.routes.ts:
    POST /nf/upload (@fastify/multipart; extrairXmls trata single XML ou ZIP via
    adm-zip; valida XSD de TODOS antes de enfileirar; enqueueNFe→409
    DUPLICATE_NF, validarNFe→422 INVALID_XML/UNSUPPORTED_SCHEMA_VERSION), GET
    /nf/jobs/:jobId (queue.getJob+getState), GET /nf (reusa listNotasFiscais,
    cursor keyset), GET /nf/:chave (getNotaFiscal + registrarConsulta
    assíncrono), /xml (storage.get), /eventos. audit.hook.ts registrarConsulta =
    fire-and-forget (void async IIFE, nunca await na resposta — regra 4).
    buildApp ganhou opções queue+storage (rotas NF só quando presentes). BUG
    CRÍTICO encontrado e corrigido: o package.json do worker tinha
    main=./dist/worker.js (entrypoint do processo) em vez de index.js — então
    importar @notagrafo/worker como lib NÃO encontrava
    enqueueNFe/XmlStorage/createNFQueue etc. Corrigido para main=index.js +
    types=index.d.ts; worker.js continua sendo o CMD do Docker. Isso afetaria a
    API e o Dockerfile. 9 testes de integração (Neo4j+Redis). test:unit 34/34,
    test:integration 37/37.
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-27T04:20:00.615Z'
  archived_at: null
---
NOTA-19 (Sprint 5, maior task): rotas de NF no @notagrafo/api. nf.routes.ts: POST /nf/upload (@fastify/multipart; extrairXmls trata single XML ou ZIP via adm-zip; valida XSD de TODOS antes de enfileirar; enqueueNFe→409 DUPLICATE_NF, validarNFe→422 INVALID_XML/UNSUPPORTED_SCHEMA_VERSION), GET /nf/jobs/:jobId (queue.getJob+getState), GET /nf (reusa listNotasFiscais, cursor keyset), GET /nf/:chave (getNotaFiscal + registrarConsulta assíncrono), /xml (storage.get), /eventos. audit.hook.ts registrarConsulta = fire-and-forget (void async IIFE, nunca await na resposta — regra 4). buildApp ganhou opções queue+storage (rotas NF só quando presentes). BUG CRÍTICO encontrado e corrigido: o package.json do worker tinha main=./dist/worker.js (entrypoint do processo) em vez de index.js — então importar @notagrafo/worker como lib NÃO encontrava enqueueNFe/XmlStorage/createNFQueue etc. Corrigido para main=index.js + types=index.d.ts; worker.js continua sendo o CMD do Docker. Isso afetaria a API e o Dockerfile. 9 testes de integração (Neo4j+Redis). test:unit 34/34, test:integration 37/37.
