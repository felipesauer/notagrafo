---
mnema:
  id: 019f0515-5b49-738d-b5af-690a9e25b7b2
  kind: observation
  content: >-
    NOTA-14 (Sprint 4): filas BullMQ + pipeline de NFe. processNFe(data,
    {driver,storage}) = validarNFe→parseNFe→mergeNotaFiscal→storage.save; lança
    em falha para o BullMQ fazer retry/backoff/DLQ. enqueueNFe bloqueia
    duplicata consultando getNotaFiscal no grafo (regra 1) e usa
    jobId=chaveAcesso. worker.ts dá boot do Worker com
    concurrency=WORKER_CONCURRENCY, roda migrations no boot. collectQueueMetrics
    expõe depth/failed via getJobCounts. BUG CRÍTICO descoberto e corrigido: os
    XSDs ficam em src/schemas mas o `tsc` NÃO copia .xsd para dist/ — então o
    validador do core compilado falhava com 'Versão 4.00 não suportada' quando
    consumido por outro pacote (worker). Corrigido: build do core agora faz `tsc
    && copy:schemas` (cpSync src/schemas→dist/schemas). Isso é o mesmo que o
    Dockerfile do plano previa (COPY packages/core/src/schemas) mas era
    necessário também no build local. Conflito de ioredis: bullmq traz
    ioredis@5.10.1; alinhei o worker a 5.10.1 (instalar 5.11 dava erro de tipos
    em ConnectionOptions). worker precisou de neo4j-driver como dep direta
    (importa o tipo Driver).
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-26T17:58:33.545Z'
  archived_at: null
---
NOTA-14 (Sprint 4): filas BullMQ + pipeline de NFe. processNFe(data, {driver,storage}) = validarNFe→parseNFe→mergeNotaFiscal→storage.save; lança em falha para o BullMQ fazer retry/backoff/DLQ. enqueueNFe bloqueia duplicata consultando getNotaFiscal no grafo (regra 1) e usa jobId=chaveAcesso. worker.ts dá boot do Worker com concurrency=WORKER_CONCURRENCY, roda migrations no boot. collectQueueMetrics expõe depth/failed via getJobCounts. BUG CRÍTICO descoberto e corrigido: os XSDs ficam em src/schemas mas o `tsc` NÃO copia .xsd para dist/ — então o validador do core compilado falhava com 'Versão 4.00 não suportada' quando consumido por outro pacote (worker). Corrigido: build do core agora faz `tsc && copy:schemas` (cpSync src/schemas→dist/schemas). Isso é o mesmo que o Dockerfile do plano previa (COPY packages/core/src/schemas) mas era necessário também no build local. Conflito de ioredis: bullmq traz ioredis@5.10.1; alinhei o worker a 5.10.1 (instalar 5.11 dava erro de tipos em ConnectionOptions). worker precisou de neo4j-driver como dep direta (importa o tipo Driver).
