---
mnema:
  id: 019f0640-d7d8-711a-923a-ca10c6e85769
  kind: observation
  content: >-
    NOTA-16 (início Sprint 5): boot Fastify 5 do @notagrafo/api. app.ts
    buildApp({rateLimit?,logger?}) monta plugins base + errorHandler + prefixo
    /api/v1; server.ts faz listen na PORT. Plugins: request-id
    (genReqId=randomUUID + @fastify/request-context; precisou de `declare module
    '@fastify/request-context' { interface RequestContextData { requestId:
    string } }` senão TS2345), swagger (openapi 3.1.0 + swagger-ui em /docs),
    rate-limit (@fastify/rate-limit com store Redis, errorResponseBuilder lança
    ApiError 429). errors.ts: envelope {error,message,detalhes?,requestId}
    (seção 8) com ApiError + errorHandler global (serializa ApiError,
    err.validation→BAD_REQUEST, 500 genérico). Testes via app.inject() (sem
    porta): app.swagger() exige await app.ready() antes. Rate-limit testado com
    Redis real (Testcontainers). api ganhou deps fastify,
    @fastify/{swagger,swagger-ui,rate-limit,request-context}, fastify-plugin,
    ioredis@5.10.1.
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-26T23:25:40.696Z'
  archived_at: null
---
NOTA-16 (início Sprint 5): boot Fastify 5 do @notagrafo/api. app.ts buildApp({rateLimit?,logger?}) monta plugins base + errorHandler + prefixo /api/v1; server.ts faz listen na PORT. Plugins: request-id (genReqId=randomUUID + @fastify/request-context; precisou de `declare module '@fastify/request-context' { interface RequestContextData { requestId: string } }` senão TS2345), swagger (openapi 3.1.0 + swagger-ui em /docs), rate-limit (@fastify/rate-limit com store Redis, errorResponseBuilder lança ApiError 429). errors.ts: envelope {error,message,detalhes?,requestId} (seção 8) com ApiError + errorHandler global (serializa ApiError, err.validation→BAD_REQUEST, 500 genérico). Testes via app.inject() (sem porta): app.swagger() exige await app.ready() antes. Rate-limit testado com Redis real (Testcontainers). api ganhou deps fastify, @fastify/{swagger,swagger-ui,rate-limit,request-context}, fastify-plugin, ioredis@5.10.1.
