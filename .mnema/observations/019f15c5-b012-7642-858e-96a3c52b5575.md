---
mnema:
  id: 019f15c5-b012-7642-858e-96a3c52b5575
  kind: observation
  content: >
    AUDITORIA EPIC-11 (2 auditores independentes, convergentes). Ponto cego:
    fixtures têm 1 item/1 CFOP, mas o seed novo gera multi-item/multi-CFOP (~1/3
    das NFs) → bugs passaram nos testes.


    JÁ CORRIGIDO: rota /empresa/:cnpj/grafo não repassava includeProdutos
    (company.routes.ts); fix+teste, revalidado na stack.


    BUGS ALTA:

    - taxByCfop (tax.queries.ts): soma nf.total_vICMS (total da NF) por CFOP;
    USA_CFOP é por (NF,CFOP) → NF com N CFOPs conta o total N vezes. Fix: somar
    por item via CONTÉM ou DISTINCT por NF.

    - getInvoice (nf.queries.ts): OPTIONAL MATCH USA_CFOP + OPTIONAL MATCH
    CONTÉM sem WITH entre eles → RETURN agrupa por cfop, N records, código usa
    records[0] → 1 CFOP arbitrário, perde os demais. Fix: collect(DISTINCT cfop)
    num WITH separado.


    FRAGILIDADES: F1 devolução soma imposto em vez de abater
    (taxSummary/taxByCfop não excluem finalidade=devolucao); F2 evento
    'importada' CREATE → não idempotente (teste não conta Evento); F3 NF stub
    (DEVOLVE) sem status entra em /stats/overview e taxSummary (falta WHERE
    status IS NOT NULL); F4 getCompanyStats sum(DISTINCT valorTotal) subconta;
    F5 formatInvoiceDetail vaza ncm.secao/capitulo + produto.cnpjEmitente; M1
    FilterSidebar não re-sincroniza deep-link na mesma rota; M2 onNodeClick
    ignora includeProdutos ao expandir.


    OK (falsos-positivos): UNION productCompanies, taxByNcm, taxSummary por UF,
    parser, round-trip URL, coerção ajv, i18n, vNF seed, regra
    devolução.</content>

    <topics>graph</topics>
  topics: []
  related_task_key: null
  created_by: 019f03ba-735c-725c-b52a-22a88c9abe61
  at: '2026-06-29T23:45:05.042Z'
  archived_at: null
---
AUDITORIA EPIC-11 (2 auditores independentes, convergentes). Ponto cego: fixtures têm 1 item/1 CFOP, mas o seed novo gera multi-item/multi-CFOP (~1/3 das NFs) → bugs passaram nos testes.

JÁ CORRIGIDO: rota /empresa/:cnpj/grafo não repassava includeProdutos (company.routes.ts); fix+teste, revalidado na stack.

BUGS ALTA:
- taxByCfop (tax.queries.ts): soma nf.total_vICMS (total da NF) por CFOP; USA_CFOP é por (NF,CFOP) → NF com N CFOPs conta o total N vezes. Fix: somar por item via CONTÉM ou DISTINCT por NF.
- getInvoice (nf.queries.ts): OPTIONAL MATCH USA_CFOP + OPTIONAL MATCH CONTÉM sem WITH entre eles → RETURN agrupa por cfop, N records, código usa records[0] → 1 CFOP arbitrário, perde os demais. Fix: collect(DISTINCT cfop) num WITH separado.

FRAGILIDADES: F1 devolução soma imposto em vez de abater (taxSummary/taxByCfop não excluem finalidade=devolucao); F2 evento 'importada' CREATE → não idempotente (teste não conta Evento); F3 NF stub (DEVOLVE) sem status entra em /stats/overview e taxSummary (falta WHERE status IS NOT NULL); F4 getCompanyStats sum(DISTINCT valorTotal) subconta; F5 formatInvoiceDetail vaza ncm.secao/capitulo + produto.cnpjEmitente; M1 FilterSidebar não re-sincroniza deep-link na mesma rota; M2 onNodeClick ignora includeProdutos ao expandir.

OK (falsos-positivos): UNION productCompanies, taxByNcm, taxSummary por UF, parser, round-trip URL, coerção ajv, i18n, vNF seed, regra devolução.</content>
<topics>graph</topics>
